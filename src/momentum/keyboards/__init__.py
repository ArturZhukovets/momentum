"""All keyboards and callback_data factories.

Submodules are per-resource (``callbacks``, ``common``, ``workout``,
``history``, ``admin``) — import the one you need, e.g.
``from momentum.keyboards import workout as kb_workout``.
"""

from __future__ import annotations
